name: Deploy

on:
  workflow_dispatch:
    inputs:
      build_number:
        description: 'Número do Build (ex: 13)'
        required: true
        type: string
      environment:
        description: 'Ambiente para deploy'
        required: true
        type: choice
        options:
          - des
          - pre
          - prod

jobs:
  deploy:
    name: Deploy to ${{ github.event.inputs.environment }}
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    
    steps:
      - name: Find Build Run ID
        id: find_run
        run: |
          echo "Procurando Build #${{ github.event.inputs.build_number }}..."
          
          # Busca o run_id do build com o número especificado
          RUN_ID=$(gh run list --workflow=build.yml --limit 100 --json databaseId,number,conclusion \
            --jq '.[] | select(.number == ${{ github.event.inputs.build_number }} and .conclusion == "success") | .databaseId')
          
          if [ -z "$RUN_ID" ]; then
            echo "❌ Build #${{ github.event.inputs.build_number }} não encontrado ou não completou com sucesso"
            exit 1
          fi
          
          echo "✅ Build #${{ github.event.inputs.build_number }} encontrado (Run ID: $RUN_ID)"
          echo "run_id=$RUN_ID" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: build-v1.0.${{ github.event.inputs.build_number }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ steps.find_run.outputs.run_id }}

      - name: Deploy to ${{ github.event.inputs.environment }}
        run: |
          echo "🚀 Deploying to ${{ github.event.inputs.environment }}"
          echo "Build Number: ${{ github.event.inputs.build_number }}"
          echo "Artifact: build-v1.0.${{ github.event.inputs.build_number }}"
          
          # Encontra o artefato baixado
          if [ -f build/info.txt ]; then
            echo "Version info:"
            cat build/info.txt
          fi
          
          if [ -f teste.txt ]; then
            cat teste.txt
          fi
          
          echo "✅ Deploy to ${{ github.event.inputs.environment }} completed!"

      - name: Deployment summary
        run: |
          echo "### 🚀 Deploy Completed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment**: ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Build Number**: ${{ github.event.inputs.build_number }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Artifact**: build-v1.0.${{ github.event.inputs.build_number }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Deployment successful!" >> $GITHUB_STEP_SUMMARY
